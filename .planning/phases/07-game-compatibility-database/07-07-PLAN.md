---
phase: 07-game-compatibility-database
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - WhiskyKit/Sources/WhiskyKit/GameDatabase/Resources/GameDB.json
autonomous: true

must_haves:
  truths:
    - "GameDB.json contains 20-50 game entries covering commonly reported games"
    - "Each entry has at least one variant with complete settings and testedWith data"
    - "Entries span multiple stores (Steam, GOG, Epic)"
    - "Entries span multiple backends (D3DMetal, DXVK, wined3d)"
    - "Entries cover all 5 rating tiers"
    - "Entries include games with known anti-cheat issues marked as notSupported/broken"
    - "Each entry has meaningful notes and known issues"
    - "Provenance is set to maintainer-verified or community for each entry"
  artifacts:
    - path: "WhiskyKit/Sources/WhiskyKit/GameDatabase/Resources/GameDB.json"
      provides: "Complete game compatibility database with 20-50 entries"
      contains: "\"version\": 1"
  key_links:
    - from: "GameDB.json entries"
      to: "BottleSettings property names"
      via: "Variant settings map to BottleSettings fields"
      pattern: "graphicsBackend.*enhancedSync"
---

<objective>
Populate GameDB.json with 20-50 verified game configuration entries covering commonly reported games, multiple stores, backends, and rating tiers.

Purpose: The database content is the actual value delivered to users. Without real entries, the UI and matching infrastructure is empty. This plan creates the initial dataset based on commonly reported games from upstream issues, existing Whisky launcher presets, and external database references.
Output: Complete GameDB.json with 20-50 entries.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-compatibility-database/07-RESEARCH.md
@WhiskyKit/Sources/WhiskyKit/Wine/LauncherPresets.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate GameDB.json with initial game entries (batch 1: 15 entries)</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/GameDatabase/Resources/GameDB.json
  </files>
  <action>
Replace the seed GameDB.json (from Plan 02) with a comprehensive database. This task creates the first batch of 15 entries.

**Data sourcing strategy (per CONTEXT.md locked decisions):**
1. **Existing Whisky knowledge:** Reference LauncherPresets.swift for launcher-specific configurations (Steam, Epic, GOG, EA, Ubisoft, Rockstar)
2. **Commonly reported games:** Based on typical Wine/macOS gaming issues from community forums and upstream issues
3. **External databases as reference:** Use WineHQ/ProtonDB compatibility data as reference to derive Whisky-specific settings

**Selection criteria (per CONTEXT.md):**
- High user demand
- No anti-cheat hard blockers (or explicitly mark as notSupported if anti-cheat blocks)
- Configs that actually change settings (not just "use defaults")
- Span common engines/launchers

**Batch 1 entries (15 games):**

Derive settings based on known Wine/macOS compatibility patterns. Each entry must have:
- Unique id (kebab-case)
- Descriptive title and store
- Rating from the 5-tier scale
- At least one variant with settings and testedWith
- Meaningful notes and known issues
- Provenance (use "community-reference" for entries derived from external data, "whisky-knowledge" for entries from launcher presets)

Games to include (covering diverse stores, backends, ratings):

**Works (D3DMetal, Steam):**
1. Stardew Valley -- simple 2D, works out of box
2. Hollow Knight -- 2D platformer, stable
3. Celeste -- 2D, minimal config needed

**Playable (various backends):**
4. Elden Ring -- D3DMetal, anti-cheat must be disabled
5. Cyberpunk 2077 -- DXVK on Intel, D3DMetal on Apple Silicon
6. The Witcher 3 -- D3DMetal, minor shader stuttering
7. Baldur's Gate 3 -- D3DMetal, needs vcrun2022
8. Hades -- D3DMetal/DXVK, audio crackling workaround
9. Dark Souls III -- D3DMetal, needs esync

**Unverified:**
10. Hogwarts Legacy -- large game, untested recently
11. Starfield -- untested, likely needs DXVK

**Broken:**
12. Red Dead Redemption 2 -- Rockstar launcher issues
13. Battlefield 2042 -- EA launcher + anti-cheat

**Not Supported:**
14. Valorant -- kernel-level Vanguard anti-cheat
15. Fortnite -- Easy Anti-Cheat (kernel)

For each entry, populate ALL schema fields:
- id, title, aliases, subtitle, store, steamAppId (for Steam games)
- rating with appropriate tier
- exeNames with the actual executable name
- pathPatterns for install directory detection
- antiCheat field for games with anti-cheat
- constraints (cpuArchitectures, minMacOSVersion, minWineVersion)
- At least 1 variant with: id, label, isDefault, whenToUse, rationale (2-3 items), settings (graphicsBackend, enhancedSync, dxvk, etc.), dllOverrides if needed, winetricksVerbs if needed, testedWith
- notes (2-3 per entry)
- knownIssues with severity and workaround
- provenance

Use ISO 8601 dates for all Date fields. Set lastTestedAt to recent dates (within last 90 days for works/playable entries, older for unverified).

Ensure the JSON is valid and well-formatted with 2-space indentation.
  </action>
  <verify>
`python3 -c "import json; json.load(open('WhiskyKit/Sources/WhiskyKit/GameDatabase/Resources/GameDB.json'))"` -- valid JSON.
`swift build --package-path WhiskyKit` -- compiles and resource loads.
  </verify>
  <done>
GameDB.json contains 15 entries covering 5 rating tiers, multiple stores, and diverse game configurations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add remaining game entries (batch 2: 10-15 entries) and validate</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/GameDatabase/Resources/GameDB.json
  </files>
  <action>
Add 10-15 more entries to reach the 25-30 total target. Focus on:

**Playable (Steam, GOG, Epic):**
16. God of War -- D3DMetal, shader cache important
17. Horizon Zero Dawn -- DXVK preferred, esync needed
18. Death Stranding -- D3DMetal, works well on Apple Silicon
19. Disco Elysium -- simple graphics, works easily
20. Sekiro: Shadows Die Twice -- D3DMetal, minor issues
21. Divinity: Original Sin 2 -- D3DMetal, GOG version
22. Total War: Warhammer III -- DXVK, needs high VRAM

**Works (simpler games):**
23. Terraria -- 2D, minimal config
24. Undertale -- very simple, defaults work
25. Cuphead -- 2D, D3DMetal works great

**Playable (Epic/GOG store diversity):**
26. The Witcher 2 -- GOG version, wined3d fallback
27. Control -- Epic version, DXVK with ray tracing off
28. Hitman 3 -- Epic, D3DMetal

**Broken/edge cases:**
29. Destiny 2 -- BattlEye anti-cheat, was supported then broken
30. PUBG -- anti-cheat blocks

For entries with multiple viable backends, add a second variant. For example, Cyberpunk 2077 could have:
- Variant 1: "Recommended (Apple Silicon)" with D3DMetal
- Variant 2: "Intel (Fallback)" with DXVK

Validate the complete JSON:
1. All entries have unique IDs
2. All required fields present
3. All rating values are valid enum cases
4. All dates are ISO 8601 format
5. All graphicsBackend values match GraphicsBackend enum raw values
6. All enhancedSync values match EnhancedSync enum raw values

Run tests to ensure the full database loads correctly:
`swift test --package-path WhiskyKit --filter GameMatcherTests` (the loader tests from Plan 02 validate the actual bundled JSON).
  </action>
  <verify>
`python3 -c "import json; d=json.load(open('WhiskyKit/Sources/WhiskyKit/GameDatabase/Resources/GameDB.json')); print(f'{len(d[\"entries\"])} entries'); assert len(d['entries']) >= 25"` -- at least 25 entries.
`swift test --package-path WhiskyKit --filter GameMatcherTests` -- all tests pass including loader tests.
`swift build --package-path WhiskyKit` -- compiles.
  </verify>
  <done>
GameDB.json contains 25-30 entries covering all 5 rating tiers, multiple stores (Steam, GOG, Epic), multiple backends (D3DMetal, DXVK, wined3d), games with and without anti-cheat, and multi-variant entries. All entries validate against the schema. Loader tests pass.
  </done>
</task>

</tasks>

<verification>
- GameDB.json is valid JSON with 25-30 entries
- `swift test --package-path WhiskyKit --filter GameMatcherTests` passes (loader validates bundled JSON)
- Entries span all 5 rating tiers
- Entries span multiple stores (Steam, GOG, Epic)
- Entries span multiple backends (D3DMetal, DXVK, wined3d)
- Anti-cheat games marked as notSupported/broken
- Multi-variant entries present
</verification>

<success_criteria>
- 25-30 game entries in GameDB.json
- All entries have complete schema fields (id, title, rating, variants, testedWith, notes, provenance)
- All enum values match Swift type raw values
- Diverse coverage: stores, backends, ratings, engines
- Loader tests pass confirming the bundled JSON decodes correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-game-compatibility-database/07-07-SUMMARY.md`
</output>
