---
phase: 10-guided-troubleshooting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/index.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/launch-crash.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/launcher-issues.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/graphics.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/audio.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/controller-input.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/install-dependencies.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/network-download.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/performance-stability.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/fragments/dependency-install.json
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/fragments/export-escalation.json
autonomous: true

must_haves:
  truths:
    - "Index JSON lists all 8 symptom categories with valid flow file references"
    - "Deep flows (launch-crash, launcher-issues, graphics, audio, install-dependencies, network-download) have 8-12 step nodes"
    - "Shallow flows (controller-input, performance-stability) have 4-5 step nodes"
    - "Every node's on map references valid node IDs within the same file or fragment"
    - "Fragment files provide reusable subflows for dependency install and export/escalation"
  artifacts:
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/index.json"
      provides: "Category metadata with entry nodes and versioning"
      contains: "categories"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/graphics.json"
      provides: "Graphics troubleshooting flow"
      contains: "graphics.backend_is"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/audio.json"
      provides: "Audio troubleshooting flow"
      contains: "audio.driver_check"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/launcher-issues.json"
      provides: "Launcher troubleshooting flow"
      contains: "launcher.type_check"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/fragments/export-escalation.json"
      provides: "Shared escalation subflow"
      contains: "export_diagnostics"
  key_links:
    - from: "flows/*.json"
      to: "fragments/export-escalation.json"
      via: "fragmentRef field on escalation nodes"
      pattern: "fragmentRef.*export-escalation"
    - from: "index.json"
      to: "flows/*.json"
      via: "flowFile field per category"
      pattern: "flowFile.*\\.json"
---

<objective>
Author all JSON flow definition files for the 8 symptom categories plus shared fragments.

Purpose: These JSON files ARE the troubleshooting knowledge base. The engine traverses them at runtime to guide users through diagnostic and fix steps. Per locked decision, flows are data-driven, not hardcoded.
Output: 11 JSON resource files in WhiskyKit/Troubleshooting/Resources/.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-guided-troubleshooting/10-RESEARCH.md
@WhiskyKit/Sources/WhiskyKit/Diagnostics/Resources/patterns.json
@WhiskyKit/Sources/WhiskyKit/Diagnostics/Resources/remediations.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index, fragments, and deep flows (launch-crash, graphics, audio, launcher-issues)</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/index.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/fragments/dependency-install.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/fragments/export-escalation.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/launch-crash.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/graphics.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/audio.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/launcher-issues.json
  </files>
  <action>
    Create the directory structure: Resources/, Resources/flows/, Resources/fragments/.

    **index.json**: Version 1. List all 8 categories with id, title, sfSymbol, flowFile, depth, and description matching the research document's index.json example exactly. Categories: launch-crash (deep), launcher-issues (deep), graphics (deep), audio (deep), controller-input (shallow), install-dependencies (deep), network-download (deep), performance-stability (shallow).

    **fragments/export-escalation.json**: Shared escalation subflow used by all flows when unresolved. Version 1, categoryId "fragment-export-escalation". Nodes: `escalation_summary` (info, phase export, "Unresolved -- here is what we tried"), `offer_enhanced_diagnostics` (check, checkId "diagnostics.can_enhance", on pass/fail), `run_enhanced_diagnostics` (fix, fixId "run-enhanced-diagnostics", enable targeted WINEDEBUG preset, on applied -> reclassify), `reclassify_after_enhanced` (check, checkId "crash.log_classify", on pass -> resolved, fail -> export_bundle), `export_bundle` (info, phase export, "Export diagnostic report"), `open_support_draft` (info, phase export, "Open support issue draft with bundle path").

    **fragments/dependency-install.json**: Shared dependency installation subflow. Version 1, categoryId "fragment-dependency-install". Nodes: `check_dependencies` (check, checkId "dependency.check_missing", params vary by caller), `install_dependency` (fix, fixId "install-dependency", requiresConfirmation true, isReversible false), `verify_dependency` (verify, "Did the installation help?"), on yes -> caller's resolved, on no -> caller's next step.

    **flows/launch-crash.json**: Deep flow (8-12 nodes). Version 1, categoryId "launch-crash", entryNodeId "check_crash_log". Flow: check_crash_log (crash.log_classify) -> if fail: show_crash_findings (info showing evidence) -> branch by crash category (GPU error -> check_graphics_backend, DLL load -> check_dependencies, access violation -> check_winetricks, other -> check_environment). check_graphics_backend (graphics.backend_is, expected dxvk) -> fix_switch_backend -> verify_fix. check_dependencies (dependency.check_missing) -> fragmentRef dependency-install. check_winetricks (winetricks.verb_check, params: verb list for common fixes) -> fix_install_verb -> verify_fix. check_environment (env.check_var) -> generic env fix. All terminal failures -> fragmentRef export-escalation. Resolved paths -> resolved node (info, phase export).

    **flows/graphics.json**: Deep flow (8-12 nodes). Follow the research document's graphics.json example closely. Entry: check_backend (graphics.backend_is, expected dxvk). check_backend fail -> fix_enable_dxvk -> verify_fix. Pass/already_configured -> check_crash_log (crash.log_classify). Crash fail -> show_crash_findings -> check_dxvk_settings. check_dxvk_settings (dxvk.settings_check, check async enabled) -> fix_enable_async -> verify_fix. Then check_game_config (game.config_available) -> suggest_game_config -> verify_fix. Final fallback -> fragmentRef export-escalation.

    **flows/audio.json**: Deep flow (8-12 nodes). Entry: check_audio_device (audio.device_check). Device error -> show_device_issue (info). Pass -> check_audio_driver (audio.driver_check, expected "pulseaudio" or per platform). Driver mismatch -> fix_audio_driver -> verify_fix. Then check_audio_registry (registry.value_check, key for DirectSound buffer). Buffer issue -> fix_buffer_size -> verify_fix. Then check_sample_rate (audio.device_check with rate params). Fallback -> fragmentRef export-escalation.

    **flows/launcher-issues.json**: Deep flow (8-12 nodes). Entry: check_launcher_type (launcher.type_check). Branch by launcher type: steam -> check_steam_process (process.running_check, process "steamwebhelper"), ea -> check_ea_env, epic -> check_epic_env, rockstar -> check_rockstar_env. For Steam: check if webhelper running -> fix_restart_webhelper, check download stall (setting.value_check for download region env). For EA/Epic/Rockstar: check env vars (env.check_var for known launcher fixes from Phase 8 LauncherFixDetail/MacOSCompatibilityFixes). Fix steps apply env overrides. All branches -> verify_fix -> if still broken -> fragmentRef export-escalation.

    Each JSON file must have valid JSON syntax. All node IDs within `on` maps must reference nodes that exist in the same file or in a referenced fragment. Include `evidenceMap` on check nodes where evidence fields should surface in the UI.
  </action>
  <verify>
    Validate all JSON files parse correctly: `python3 -c "import json, glob; [json.load(open(f)) for f in glob.glob('WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/**/*.json', recursive=True)]"`. Verify node ID cross-references: for each node's `on` values, the target node ID exists in the same file's nodes map OR the node has a `fragmentRef`.
  </verify>
  <done>
    index.json, 2 fragments, and 4 deep flows (launch-crash, graphics, audio, launcher-issues) exist with valid JSON and internally-consistent node references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remaining deep flows and shallow flows</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/install-dependencies.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/network-download.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/controller-input.json
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/flows/performance-stability.json
  </files>
  <action>
    **flows/install-dependencies.json**: Deep flow (8-10 nodes). Version 1, categoryId "install-dependencies", entryNodeId "check_installed_verbs". Flow: check_installed_verbs (winetricks.verb_check, params: common runtime verbs like vcrun2019, dotnet48, d3dx9, dxvk) -> branch on fail: show_missing_deps (info listing what is missing from evidence) -> fix_install_deps (fix, fixId "install-winetricks-verb", requiresConfirmation true, isReversible false) -> verify_install. If still failing -> check for specific DLL presence (dependency.check_missing, params: specific DLLs like msvcr140.dll, d3d9.dll) -> show_dll_findings -> suggest manual installation or alternate verb. If pass on initial check -> check_game_config (game.config_available, may suggest known-good verb set) -> apply or skip -> resolved. Terminal failure -> fragmentRef export-escalation.

    **flows/network-download.json**: Deep flow (8-10 nodes). Version 1, categoryId "network-download", entryNodeId "check_launcher_type". Flow: check_launcher_type (launcher.type_check) -> branch: Steam -> check_steam_download (process.running_check, steam webhelper processes), non-Steam -> check_general_network. For Steam: check_steam_download -> if many processes -> fix_restart_steam (fix, kill wineserver + relaunch) -> verify. If few processes -> fix_download_region (info suggesting manual Steam region change, not automatable) -> verify. check_general_network -> check_environment (env.check_var for proxy/DNS vars) -> fix or info nodes. check_wine_dns (registry.value_check for DNS settings) -> fix if needed. Final -> fragmentRef export-escalation.

    **flows/controller-input.json**: Shallow flow (4-5 nodes). Version 1, categoryId "controller-input", entryNodeId "check_controller_env". Flow: check_controller_env (env.check_var, params: SDL_GAMECONTROLLERCONFIG) -> if set -> info explaining how to clear/modify -> verify. If not set -> check_setting (setting.value_check, params: controllerCompatibilityMode/xinputOverride) -> fix_enable_compat (fix, enable controller compatibility mode) -> verify. If still not working -> info node with guidance on macOS System Settings Bluetooth and re-pairing, then escalate to export. Per locked decision: lighter flow, escalate earlier.

    **flows/performance-stability.json**: Shallow flow (4-5 nodes). Version 1, categoryId "performance-stability", entryNodeId "check_enhanced_sync". Flow: check_enhanced_sync (setting.value_check, params: enhancedSync) -> if none -> fix_enable_esync (fix setting) -> verify. Then check_hud (dxvk.settings_check, check FPS/frame time HUD for diagnosis) -> info_enable_hud (suggest enabling DXVK HUD temporarily). If still stutter -> check_game_config (game.config_available) -> suggest known config. Final -> fragmentRef export-escalation. Per locked decision: lighter flow, escalate earlier.

    All flows must: have version 1, match their categoryId to index.json, have valid entryNodeId, have all on-map targets resolve within the file or to fragment nodes. Include user-friendly titles and descriptions on every node.
  </action>
  <verify>
    Validate all JSON files parse: `python3 -c "import json, glob; [json.load(open(f)) for f in glob.glob('WhiskyKit/Sources/WhiskyKit/Troubleshooting/Resources/**/*.json', recursive=True)]"`. Count total nodes across all flow files -- deep flows should have 8-12 nodes each, shallow flows 4-5 each.
  </verify>
  <done>
    All 8 category flow files and 2 fragment files exist. Deep flows have 8-12 nodes, shallow flows have 4-5 nodes. All JSON is valid and node references are internally consistent.
  </done>
</task>

</tasks>

<verification>
- All 11 JSON files exist and parse without errors
- index.json references all 8 flow files
- Every flow file has version, categoryId, entryNodeId, and nodes
- Every `on` target resolves within the file or to a fragment
- Deep flows: 8-12 nodes each (launch-crash, graphics, audio, launcher-issues, install-dependencies, network-download)
- Shallow flows: 4-5 nodes each (controller-input, performance-stability)
- Fragment files provide shared escalation and dependency install subflows
</verification>

<success_criteria>
Complete JSON flow definition library covering all 8 symptom categories with appropriate depth per locked tiered flow decisions. Each flow uses stable checkId references matching the check implementations that will be built in Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/10-guided-troubleshooting/10-02-SUMMARY.md`
</output>
