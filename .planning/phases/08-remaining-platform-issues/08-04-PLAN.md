---
phase: 08-remaining-platform-issues
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Whisky/Views/Bottle/LauncherConfigSection.swift
  - Whisky/Views/Common/LaunchTimeBanner.swift
  - Whisky/Views/Common/StatusToast.swift
autonomous: true

must_haves:
  truths:
    - "Launch-time banner shows what launcher fixes were applied with View Details link"
    - "Launcher Config section shows EnvironmentBuilder provenance with reason strings"
    - "All 7 LauncherType entries display their fix descriptions in the Config section"
  artifacts:
    - path: "Whisky/Views/Bottle/LauncherConfigSection.swift"
      provides: "Provenance display showing per-env-var reasons"
      contains: "provenance"
    - path: "Whisky/Views/Common/LaunchTimeBanner.swift"
      provides: "Launch-time banner for applied fixes notification"
      contains: "LaunchTimeBanner"
    - path: "Whisky/Views/Common/StatusToast.swift"
      provides: "launcherFixes toast style for launch-time notification"
      contains: "launcherFixes"
  key_links:
    - from: "LaunchTimeBanner.swift"
      to: "LauncherPresets.swift"
      via: "fixDetails() for banner content"
      pattern: "fixDetails"
    - from: "LauncherConfigSection.swift"
      to: "EnvironmentBuilder.swift"
      via: "provenance reason display"
      pattern: "reason"
---

<objective>
Build launcher guidance UI: launch-time fix banner and Config section with provenance display.

Purpose: Per user decision (three-tier discovery model), launcher fixes surface at: (1) launch-time banner/toast showing fixes applied, (2) Bottle Config Launcher section as source of truth with provenance. This plan implements tiers 1 and 2 using the structured fix metadata from Plan 01. No separate "launcher fixes screen" per user decision.

Output: LaunchTimeBanner view, extended LauncherConfigSection with provenance, launcherFixes toast style.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-remaining-platform-issues/08-01-SUMMARY.md
@Whisky/Views/Bottle/LauncherConfigSection.swift
@Whisky/Views/Common/StatusToast.swift
@WhiskyKit/Sources/WhiskyKit/Wine/LauncherPresets.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add launcherFixes toast style and create LaunchTimeBanner</name>
  <files>
    Whisky/Views/Common/StatusToast.swift
    Whisky/Views/Common/LaunchTimeBanner.swift
  </files>
  <action>
In StatusToast.swift:
1. Add a `.launcherFixes` case to `ToastStyle` enum. Icon: "wrench.and.screwdriver.fill", color: .orange. Accessibility label: "Launcher fixes applied".
2. This follows the existing pattern (success/error/info) with just a new case.

Create `Whisky/Views/Common/LaunchTimeBanner.swift` with GPL v3 header:

1. Define `LaunchTimeBannerData: Equatable, Sendable` struct with:
   - `launcherName: String`
   - `fixCategories: [String]` (e.g. ["locale", "sandbox", "network timeout"])
   - `fixCount: Int`
   - A static `from(launcher: LauncherType) -> LaunchTimeBannerData` factory that calls `launcher.fixDetails()` and derives categories from FixCategory raw values, deduped.

2. Define `LaunchTimeBanner: View` that displays:
   - Orange-tinted banner (not a blocking dialog -- per user specifics: "feels like a status notification")
   - "Launcher fixes applied: {launcherName}" as title
   - "{fixCount} settings configured ({categories})" as subtitle
   - "View Details" link button that posts a Notification (e.g. `.openLauncherConfig`) to deep-link to the Launcher section in ConfigView
   - Auto-dismiss after 5 seconds (matching crash banner pattern from Phase 5)
   - Transition: slide in from top + opacity

3. Define `LaunchTimeBannerModifier: ViewModifier` that:
   - Takes a `@Binding var bannerData: LaunchTimeBannerData?`
   - Shows the banner as an overlay at top when bannerData is non-nil
   - Schedules auto-dismiss after 5s
   - Allows tap-to-dismiss

This surfaces tier 1 of the three-tier discovery model. The banner should feel lightweight, not blocking.
  </action>
  <verify>
`xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds. `grep -r "LaunchTimeBanner" Whisky/Views/Common/` returns hits. `grep "launcherFixes" Whisky/Views/Common/StatusToast.swift` returns hits.
  </verify>
  <done>
LaunchTimeBanner shows "Launcher fixes applied: Steam" with fix count and category summary. Auto-dismisses after 5s. launcherFixes toast style available for fallback toast usage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance LauncherConfigSection with provenance display</name>
  <files>
    Whisky/Views/Bottle/LauncherConfigSection.swift
  </files>
  <action>
Extend LauncherConfigSection to show EnvironmentBuilder provenance with reasons (tier 2 of discovery model):

1. Add a new expandable section within the existing launcher config (below the existing controls, before the diagnostics button) titled "Applied Fixes" that appears when launcher compatibility mode is enabled:
   - For the detected launcher: call `launcher.fixDetails()` to get the structured fix list
   - Display each fix as a row: env var key on left, reason on right (secondary text)
   - Group by FixCategory with small section headers
   - Each row shows the env var name in monospace and the reason in caption font

2. Add a "Platform Fixes" subsection showing macOS compatibility fixes:
   - Call `MacOSCompatibility.activeFixes()` (from Plan 01) to get version-gated fixes
   - Display each fix with its reason and "Applied because macOS >= {version}" provenance
   - This makes managed fixes transparent per user decision

3. Style: Use a DisclosureGroup labeled "Active Environment Overrides" (collapsed by default) to avoid cluttering the main launcher config. Inside, use a grouped list with lock icons for managed/non-editable entries (following the DLLOverrideEditor managed pattern from Phase 2).

4. Remove the "Generate Diagnostics Report" button and LauncherDiagnostics call from this section -- that functionality is superseded by the Phase 5 diagnostics pipeline. Replace with a link to the Diagnostics section in ConfigView.

5. Keep all existing controls (compatibility mode toggle, detection mode, locale, GPU spoofing, network timeout, auto-DXVK) unchanged.
  </action>
  <verify>
`xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds. The LauncherConfigSection compiles with the new provenance display.
  </verify>
  <done>
LauncherConfigSection shows structured fix details grouped by category with reason strings. macOS compatibility fixes display "Applied because macOS >= X.Y" provenance. Users can see exactly what settings are managed and why.
  </done>
</task>

</tasks>

<verification>
- `xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds
- LaunchTimeBanner renders launcher name and fix summary
- LauncherConfigSection shows provenance for launcher and macOS fixes
- Toast .launcherFixes style renders with orange icon
</verification>

<success_criteria>
- Launch-time banner shows "Launcher fixes applied: {name}" with auto-dismiss (tier 1)
- Launcher Config section shows per-fix provenance with reasons (tier 2)
- macOS compatibility fixes shown with version-gated provenance
- No separate "launcher fixes screen" (per user decision)
- All 7 LauncherType entries display their fixes
</success_criteria>

<output>
After completion, create `.planning/phases/08-remaining-platform-issues/08-04-SUMMARY.md`
</output>
