---
phase: 09-ui-ux-feature-requests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WhiskyKit/Sources/WhiskyKit/Whisky/BottleSettings.swift
  - WhiskyKit/Sources/WhiskyKit/Whisky/ProgramOverrides.swift
  - WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift
  - WhiskyKit/Sources/WhiskyKit/Whisky/BottleDisplayConfig.swift
autonomous: true

must_haves:
  truths:
    - "Bottle settings include virtual desktop toggle, resolution preset, and custom resolution dimensions"
    - "Per-program overrides can override virtual desktop and resolution settings"
    - "Wine registry helpers can enable/disable virtual desktop and set resolution via Explorer registry keys"
  artifacts:
    - path: "WhiskyKit/Sources/WhiskyKit/Whisky/BottleDisplayConfig.swift"
      provides: "BottleDisplayConfig with virtualDesktop, resolution preset, custom dimensions"
      contains: "BottleDisplayConfig"
    - path: "WhiskyKit/Sources/WhiskyKit/Whisky/ProgramOverrides.swift"
      provides: "Display override fields on ProgramOverrides"
      contains: "virtualDesktopEnabled"
    - path: "WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift"
      provides: "enableVirtualDesktop and disableVirtualDesktop registry methods"
      contains: "enableVirtualDesktop"
  key_links:
    - from: "WhiskyKit/Sources/WhiskyKit/Whisky/BottleSettings.swift"
      to: "WhiskyKit/Sources/WhiskyKit/Whisky/BottleDisplayConfig.swift"
      via: "displayConfig stored property with proxy properties"
      pattern: "displayConfig"
    - from: "WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift"
      to: "WhiskyKit/Sources/WhiskyKit/Whisky/BottleDisplayConfig.swift"
      via: "ResolutionPreset used in enableVirtualDesktop"
      pattern: "ResolutionPreset"
---

<objective>
Create resolution control data model with virtual desktop registry helpers.

Purpose: Build the data foundation for display resolution control (FEAT-03). This includes the BottleDisplayConfig settings struct, ResolutionPreset enum, virtual desktop Wine registry operations, and per-program override fields. The UI layer is in Plan 03.
Output: BottleDisplayConfig, ResolutionPreset, Wine registry helpers for virtual desktop, ProgramOverrides display fields.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@WhiskyKit/Sources/WhiskyKit/Whisky/BottleSettings.swift
@WhiskyKit/Sources/WhiskyKit/Whisky/ProgramOverrides.swift
@WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift
@WhiskyKit/Sources/WhiskyKit/Whisky/BottleGraphicsConfig.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: BottleDisplayConfig and ResolutionPreset data model</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/Whisky/BottleDisplayConfig.swift
    WhiskyKit/Sources/WhiskyKit/Whisky/BottleSettings.swift
  </files>
  <action>
1. **Create BottleDisplayConfig.swift** following the BottleGraphicsConfig pattern exactly (module-scope enums, defensive decoding, private stored property with proxy properties):

   a. **ResolutionPreset** enum at module scope (public, Codable, Equatable, Sendable, CaseIterable):
      - Cases: `.r1280x720`, `.r1600x900`, `.r1920x1080`, `.r2560x1440`, `.r3840x2160`, `.matchDisplay`, `.custom`
      - Each case has a `label: String` computed property (e.g., "1280 x 720", "1920 x 1080 (Default)", "Match Mac Display", "Custom")
      - Each case has a `dimensions: (width: Int, height: Int)?` computed property returning the pixel dimensions (nil for `.matchDisplay` and `.custom`)
      - String raw values for Codable.

   b. **BottleDisplayConfig** struct (public, Codable, Equatable):
      - `var virtualDesktopEnabled: Bool = false`
      - `var resolutionPreset: ResolutionPreset = .r1920x1080`
      - `var customWidth: Int = 1920`
      - `var customHeight: Int = 1080`
      - Defensive decoding with `decodeIfPresent` and defaults for every field.
      - Computed property `effectiveResolution: (width: Int, height: Int)` that resolves the preset to actual pixel values. For `.matchDisplay`, return (1920, 1080) as fallback (actual screen query happens in app layer). For `.custom`, return (customWidth, customHeight). For named presets, return preset dimensions.

2. **Wire into BottleSettings.swift:**
   - Add `private var displayConfig: BottleDisplayConfig` stored property (following graphics/audio config pattern).
   - Decode with `decodeIfPresent` defaulting to `BottleDisplayConfig()`.
   - Add public proxy properties:
     - `virtualDesktopEnabled: Bool { get/set }`
     - `resolutionPreset: ResolutionPreset { get/set }`
     - `customResolutionWidth: Int { get/set }`
     - `customResolutionHeight: Int { get/set }`
   - Add CodingKeys entry for displayConfig.
  </action>
  <verify>`swift build --package-path WhiskyKit` succeeds. `swift test --package-path WhiskyKit` passes.</verify>
  <done>BottleDisplayConfig struct with ResolutionPreset enum exists. BottleSettings exposes virtual desktop and resolution properties.</done>
</task>

<task type="auto">
  <name>Task 2: Virtual desktop registry helpers and ProgramOverrides display fields</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift
    WhiskyKit/Sources/WhiskyKit/Whisky/ProgramOverrides.swift
  </files>
  <action>
1. **WineRegistry.swift:** Add virtual desktop registry operations:
   - Add RegistryKey cases:
     - `.explorer = "HKCU\\Software\\Wine\\Explorer"`
     - `.explorerDesktops = "HKCU\\Software\\Wine\\Explorer\\Desktops"`
   - Add `Wine.enableVirtualDesktop(bottle:resolution:)` async throws:
     - Write `Desktop = "Default"` (REG_SZ) to explorer key
     - Write `Default = "{width}x{height}"` (REG_SZ) to explorerDesktops key
     - Parameter `resolution` is a String like "1920x1080"
   - Add `Wine.disableVirtualDesktop(bottle:)` async throws:
     - Delete the `Desktop` value from explorer key using `reg delete ... -v Desktop -f`
   - Add `Wine.queryVirtualDesktop(bottle:)` async throws -> String?:
     - Query explorer key for "Desktop" value. If present, query explorerDesktops for "Default" to get resolution string. Return nil if not enabled.

2. **ProgramOverrides.swift:** Add display override fields in a new `// MARK: - Display` section:
   - `public var virtualDesktopEnabled: Bool?` (nil = inherit from bottle)
   - `public var resolutionPreset: ResolutionPreset?` (nil = inherit from bottle)
   - `public var customResolutionWidth: Int?`
   - `public var customResolutionHeight: Int?`
   - Update `isEmpty` check to include these fields.
   - Use `decodeIfPresent` in init(from:) for backward compatibility.
  </action>
  <verify>`swift build --package-path WhiskyKit` succeeds. `swift test --package-path WhiskyKit` passes. Grep for `enableVirtualDesktop` in WineRegistry.swift confirms method exists.</verify>
  <done>Wine registry helpers for virtual desktop exist. ProgramOverrides includes display override fields with nil=inherit semantics.</done>
</task>

</tasks>

<verification>
- Build: `swift build --package-path WhiskyKit`
- Tests: `swift test --package-path WhiskyKit`
- Verify new types: grep for `BottleDisplayConfig`, `ResolutionPreset`, `enableVirtualDesktop` in WhiskyKit sources
- Verify ProgramOverrides: grep for `virtualDesktopEnabled` in ProgramOverrides.swift
</verification>

<success_criteria>
- BottleDisplayConfig with 7 resolution presets and custom option is Codable and wired into BottleSettings
- Virtual desktop can be enabled/disabled via Wine registry helpers
- Per-program display overrides follow nil=inherit pattern consistent with existing override groups
- All existing tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-feature-requests/09-02-SUMMARY.md`
</output>
