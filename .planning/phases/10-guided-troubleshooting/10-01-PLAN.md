---
phase: 10-guided-troubleshooting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/FlowDefinition.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/FlowIndex.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/SymptomCategory.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/CheckResult.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingCheck.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/CheckContext.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/FixAttempt.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/PreflightData.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/EntryContext.swift
  - WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingSession.swift
  - WhiskyKit/Package.swift
autonomous: true

must_haves:
  truths:
    - "All Codable model types compile under Swift 6 strict concurrency"
    - "FlowDefinition can decode JSON matching the locked check binding schema"
    - "SymptomCategory has exactly 8 named categories matching locked decisions"
    - "CheckResult normalizes outcomes to pass/fail/already_configured/unknown/error"
    - "TroubleshootingSession captures full wizard state for persistence"
  artifacts:
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/FlowDefinition.swift"
      provides: "FlowDefinition and FlowStepNode Codable types"
      contains: "struct FlowDefinition"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/SymptomCategory.swift"
      provides: "8 symptom categories enum"
      contains: "enum SymptomCategory"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/CheckResult.swift"
      provides: "Normalized check outcome with evidence"
      contains: "enum CheckOutcome"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingCheck.swift"
      provides: "Protocol for check implementations"
      contains: "protocol TroubleshootingCheck"
    - path: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingSession.swift"
      provides: "Session state for wizard persistence"
      contains: "struct TroubleshootingSession"
    - path: "WhiskyKit/Package.swift"
      provides: "SPM resource declaration for Troubleshooting/Resources/"
      contains: "Troubleshooting/Resources/"
  key_links:
    - from: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/FlowDefinition.swift"
      to: "Troubleshooting/Resources/*.json"
      via: "Codable JSON decoding"
      pattern: "FlowStepNode.*checkId"
    - from: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingCheck.swift"
      to: "WhiskyKit/Sources/WhiskyKit/Troubleshooting/CheckResult.swift"
      via: "protocol return type"
      pattern: "func run.*CheckResult"
---

<objective>
Define all core Codable data models and protocols for the troubleshooting flow engine.

Purpose: Establish the type foundation that the engine, checks, JSON flows, session persistence, and UI all depend on. Every other Phase 10 plan imports these types.
Output: 10 new Swift files in WhiskyKit/Troubleshooting/ plus Package.swift update for SPM resources.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-guided-troubleshooting/10-RESEARCH.md
@WhiskyKit/Package.swift
@WhiskyKit/Sources/WhiskyKit/Diagnostics/ConfidenceTier.swift
@WhiskyKit/Sources/WhiskyKit/Audio/AudioTroubleshootingStep.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flow definition models, symptom category, and check protocol</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/FlowDefinition.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/FlowIndex.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/SymptomCategory.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/CheckResult.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingCheck.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/CheckContext.swift
  </files>
  <action>
    Create the Troubleshooting/ directory under WhiskyKit/Sources/WhiskyKit/.

    **FlowDefinition.swift**: Define `FlowDefinition: Codable, Sendable` with `version: Int`, `categoryId: String`, `nodes: [String: FlowStepNode]`, `entryNodeId: String`. Define `FlowStepNode: Codable, Sendable, Identifiable` with fields: `id: String`, `type: NodeType` (enum: check/fix/info/verify/branch), `phase: FlowPhase` (enum: symptom/checks/fix/verify/export), `title: String?`, `description: String?`, `checkId: String?`, `params: [String: String]?`, `on: [String: String]?` (outcome to next node ID), `evidenceMap: [String: String]?`, `fixId: String?`, `fixPreview: FixPreviewData?`, `isReversible: Bool?`, `requiresConfirmation: Bool?`, `fragmentRef: String?`. Define `FixPreviewData: Codable, Sendable` with `settingName: String`, `currentValueKey: String?`, `newValue: String`, `scope: String`. Use `decodeIfPresent` for all optional fields per locked decision on defensive decoding. NodeType and FlowPhase are String raw-value enums for direct JSON mapping.

    **FlowIndex.swift**: Define `FlowIndex: Codable, Sendable` with `version: Int`, `categories: [FlowCategoryEntry]`. Define `FlowCategoryEntry: Codable, Sendable, Identifiable` with `id: String`, `title: String`, `sfSymbol: String`, `flowFile: String`, `depth: FlowDepth`, `description: String`. `FlowDepth` is an enum with cases `deep` and `shallow` (String raw values).

    **SymptomCategory.swift**: Define `SymptomCategory: String, CaseIterable, Codable, Sendable` enum with exactly 8 cases matching locked decisions: `launchCrash`, `launcherIssues`, `graphics`, `audio`, `controllerInput`, `installDependencies`, `networkDownload`, `performanceStability`. Add computed `flowFileName: String` returning the JSON file name (e.g., "launch-crash.json"). Add computed `displayTitle: String` and `sfSymbol: String` matching the index.json values. Add an `other` case as fallback only if needed per locked decision (include it as ninth case).

    **CheckResult.swift**: Define `CheckResult: Sendable, Codable` struct with `outcome: CheckOutcome`, `evidence: [String: String]`, `summary: String`, `confidence: ConfidenceTier?` (reuse existing ConfidenceTier from Phase 5). `CheckOutcome: String, Sendable, Codable` enum with cases: `pass`, `fail`, `alreadyConfigured` (raw value "already_configured"), `unknown`, `error`.

    **TroubleshootingCheck.swift**: Define `TroubleshootingCheck: Sendable` protocol with `var checkId: String { get }` and `func run(params: [String: String], context: CheckContext) async -> CheckResult`.

    **CheckContext.swift**: Define `CheckContext: Sendable` struct with `bottle: Bottle`, `program: Program?`, `preflight: PreflightData`, `session: TroubleshootingSession`. Since Bottle and Program are @MainActor classes, store their URLs instead: `bottleURL: URL`, `bottleName: String`, `programURL: URL?`, `programName: String?`, `preflight: PreflightData`. Add a factory method `@MainActor static func create(bottle: Bottle, program: Program?, preflight: PreflightData, session: TroubleshootingSession) -> CheckContext` that captures the needed values.

    All files get the GPL v3 header with target "WhiskyKit". All types are public. Follow existing naming conventions (PascalCase types, camelCase properties).
  </action>
  <verify>
    Run `swift build --package-path WhiskyKit` -- should compile (may warn about unused types, that is fine).
  </verify>
  <done>
    FlowDefinition, FlowIndex, SymptomCategory, CheckResult, TroubleshootingCheck, and CheckContext types exist and compile under Swift 6 strict concurrency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Session model, fix attempt, preflight data, entry context, and Package.swift update</name>
  <files>
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/FixAttempt.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/PreflightData.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/EntryContext.swift
    WhiskyKit/Sources/WhiskyKit/Troubleshooting/TroubleshootingSession.swift
    WhiskyKit/Package.swift
  </files>
  <action>
    **FixAttempt.swift**: Define `FixAttempt: Codable, Sendable` struct with `fixId: String`, `timestamp: Date`, `beforeValue: String?`, `afterValue: String?`, `result: FixResult`. `FixResult: String, Codable, Sendable` enum with cases: `pending`, `applied`, `verified`, `failed`, `undone`. This extends the pattern from Phase 6's TroubleshootingFixAttempt but adds before/after and result tracking per locked decisions.

    **PreflightData.swift**: Define `PreflightData: Codable, Sendable` struct with: `bottleURL: URL`, `bottleName: String`, `programURL: URL?`, `programName: String?`, `launcherType: String?`, `isWineserverRunning: Bool`, `processCount: Int`, `recentLogURL: URL?`, `lastExitCode: Int32?`, `audioDeviceName: String?`, `audioTransportType: String?`, `graphicsBackend: String`, `collectedAt: Date`. All fields are simple Codable types. This is a snapshot struct -- the PreflightCollector that populates it lives in Plan 03.

    **EntryContext.swift**: Define `EntryContext: Sendable` enum with cases: `program(programURL: URL, bottleURL: URL)` (primary entry from Program view), `launchFailure(programURL: URL, bottleURL: URL, evidence: [String: String])` (from crash banner with prefilled findings), `bottleDiagnostics(bottleURL: URL)` (from Config diagnostics section), `helpMenu(bottleURL: URL, programURL: URL?)` (from Help menu target picker). Add computed property `bottleURL: URL`, `programURL: URL?`, `initialPhase: TroubleshootingSession.SessionPhase` (launchFailure starts at .checks, bottleDiagnostics at .checks, others at .symptom per locked context-aware start decisions).

    **TroubleshootingSession.swift**: Define `TroubleshootingSession: Codable, Sendable` struct with: `id: UUID` (default UUID()), `bottleURL: URL?`, `programURL: URL?`, `symptomCategory: SymptomCategory?`, `phase: SessionPhase` (default .symptom), `stepHistory: [CompletedStep]` (default []), `currentNodeId: String?`, `currentFlowCategoryId: String?`, `checkResults: [String: CheckResult]` (default [:]), `fixAttempts: [FixAttempt]` (default []), `branchDecisions: [BranchDecision]` (default []), `outcome: SessionOutcome?`, `createdAt: Date` (default Date()), `lastUpdatedAt: Date` (default Date()), `preflightSnapshot: PreflightData?`. Define nested types: `SessionPhase: String, Codable, Sendable` enum (symptom/checks/fix/verify/export/escalation), `SessionOutcome: String, Codable, Sendable` enum (resolved/unresolved/abandoned), `CompletedStep: Codable, Sendable` struct (nodeId: String, phase: SessionPhase, title: String?, checkResult: CheckResult?, isSuperseded: Bool), `BranchDecision: Codable, Sendable` struct (fromNodeId: String, toNodeId: String, reason: String?, timestamp: Date). Add computed `failedFixCount: Int` counting fixAttempts where result == .failed. Add mutating methods: `pushStep(_ node: FlowStepNode)`, `recordCheckResult(nodeId: String, result: CheckResult)`, `recordFixAttempt(_ attempt: FixAttempt)`, `recordBranch(from: String, to: String, reason: String?)`.

    **Package.swift**: Read the existing Package.swift. Add `"Troubleshooting/Resources/"` to the resources array in the WhiskyKit target, alongside existing `"Diagnostics/Resources/"` and `"GameDatabase/Resources/"` entries. Use `.process("Troubleshooting/Resources/")`.

    All files get GPL v3 header. All types public.
  </action>
  <verify>
    Run `swift build --package-path WhiskyKit` -- all types compile. Verify Package.swift includes the new resource path.
  </verify>
  <done>
    FixAttempt, PreflightData, EntryContext, and TroubleshootingSession types exist and compile. Package.swift declares Troubleshooting/Resources/ for SPM resource processing.
  </done>
</task>

</tasks>

<verification>
- `swift build --package-path WhiskyKit` succeeds
- All 10 new .swift files exist in WhiskyKit/Sources/WhiskyKit/Troubleshooting/
- Package.swift includes `Troubleshooting/Resources/` in resources array
- No SwiftLint file_header violations (all files have GPL v3 header)
</verification>

<success_criteria>
All core Codable types and protocols for the troubleshooting system compile under Swift 6. Package.swift is updated for resource bundling. No runtime code yet -- this is pure type definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-guided-troubleshooting/10-01-SUMMARY.md`
</output>
