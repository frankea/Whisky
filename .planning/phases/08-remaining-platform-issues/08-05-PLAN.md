---
phase: 08-remaining-platform-issues
plan: 05
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - Whisky/Views/Bottle/InputConfigSection.swift
  - Whisky/Views/Programs/ProgramOverrideSettingsView.swift
autonomous: true

must_haves:
  truths:
    - "Connected Controllers subpanel shows name, type badge, connection badge, and battery"
    - "Bluetooth inline warning displayed when wireless controllers connected"
    - "XInput vs Native labels toggle available in controller settings"
    - "Per-program input overrides available via inherit/override pattern"
  artifacts:
    - path: "Whisky/Views/Bottle/InputConfigSection.swift"
      provides: "Connected Controllers subpanel with live discovery"
      contains: "ControllerMonitor"
    - path: "Whisky/Views/Programs/ProgramOverrideSettingsView.swift"
      provides: "Per-program input override section"
      contains: "inputConfig"
  key_links:
    - from: "InputConfigSection.swift"
      to: "ControllerMonitor.swift"
      via: "ControllerMonitor for live controller list"
      pattern: "controllerMonitor"
    - from: "InputConfigSection.swift"
      to: "BottleInputConfig.swift"
      via: "useButtonLabels binding for XInput/Native toggle"
      pattern: "useButtonLabels"
---

<objective>
Build controller panel UI: Connected Controllers subpanel and per-program input overrides.

Purpose: Per user decision, the InputConfigSection gets a Connected Controllers subpanel (collapsed by default) showing connected controllers with type/connection/battery badges. The existing SDL toggle settings remain, plus the new XInput vs Native labels toggle from Plan 02. Per-program input overrides use the existing inherit/override pattern.

Output: Enhanced InputConfigSection with controller subpanel, updated ProgramOverrideSettingsView with input overrides.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-remaining-platform-issues/08-02-SUMMARY.md
@Whisky/Views/Bottle/InputConfigSection.swift
@Whisky/Utils/ControllerMonitor.swift
@Whisky/Views/Programs/ProgramOverrideSettingsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Connected Controllers subpanel to InputConfigSection</name>
  <files>
    Whisky/Views/Bottle/InputConfigSection.swift
  </files>
  <action>
Enhance InputConfigSection with a Connected Controllers subpanel:

1. Add `@StateObject private var controllerMonitor = ControllerMonitor()` to InputConfigSection.
2. Start/stop monitoring via `.onAppear { controllerMonitor.startMonitoring() }` and `.onDisappear { controllerMonitor.stopMonitoring() }`.

3. Add a "Connected Controllers" DisclosureGroup (collapsed by default per user decision) inside the existing VStack, positioned after the main controller compatibility toggle and before the existing SDL settings:

   Inside the DisclosureGroup:
   - If `controllerMonitor.controllers.isEmpty`: show "No controllers detected" + troubleshooting hint ("Try connecting via USB or check System Settings > Bluetooth") per user decision.
   - If controllers exist: ForEach over controllerMonitor.controllers, each row showing:
     * Controller name (bold)
     * Type badge: HStack with SF Symbol + "PlayStation"/"Xbox"/"Generic" text (use controller.typeBadge)
     * Connection badge: SF Symbol + "USB"/"Bluetooth" text (use controller.connectionType)
     * Battery level: if available, show as "Battery: {percent}%" with optional charging indicator
     * Use a card-like row style similar to macOS System Settings Bluetooth device list (per user specifics)
   - If any controller has connectionType == .bluetooth: show an inline warning banner "Bluetooth dropouts can break input; USB is more reliable" (per user decision, amber/orange tint)

   Action buttons below the controller list:
   - "Refresh" button calling `controllerMonitor.refresh()`
   - "Copy Controller Info" button that copies controller details to clipboard (for diagnostics)
   - Note: "Test Input" deferred -- requires live input viewer which is complex; instead add a "Test Input" note pointing to System Settings > Game Controllers

4. Add the new "Use Native Button Labels" toggle (binding to `bottle.settings.useButtonLabels`) alongside the existing toggles. Help text: "Preserves physical button positions (Cross/Circle) for PlayStation controllers instead of XInput layout (A/B/X/Y)."

5. Keep all existing toggles (Controller Compatibility Mode, Disable HIDAPI, Allow Background Events, Use Native Button Labels which was already `disableControllerMapping` -- rename the existing "Use Native Button Labels" toggle to clarify: the existing `disableControllerMapping` becomes the general mapping toggle, the new `useButtonLabels` is specifically for button label display).

6. Show last refreshed timestamp at the bottom of the subpanel in caption font.
  </action>
  <verify>
`xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds. InputConfigSection compiles with the ControllerMonitor integration.
  </verify>
  <done>
Connected Controllers subpanel shows live controller list with type/connection/battery badges. Bluetooth warning displayed for wireless controllers. XInput vs Native labels toggle available. Refresh and Copy Controller Info buttons work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add per-program input overrides to ProgramOverrideSettingsView</name>
  <files>
    Whisky/Views/Programs/ProgramOverrideSettingsView.swift
  </files>
  <action>
Extend ProgramOverrideSettingsView with an input override group:

1. Locate the existing override groups in ProgramOverrideSettingsView (wine, graphics, performance, etc.). Add a new "Input" override group following the same inherit/override pattern used by graphics overrides.

2. The input override group should expose:
   - Controller Compatibility Mode (Bool toggle)
   - Disable HIDAPI (Bool toggle)
   - Allow Background Events (Bool toggle)
   - Use Native Button Labels (Bool toggle, maps to useButtonLabels)
   Each field uses the nil-check inherit/override pattern: nil = inherit from bottle, non-nil = override.

3. If ProgramOverrides doesn't already have input fields, add optional fields to ProgramOverrides for the input settings (controllerCompatibilityMode: Bool?, disableHIDAPI: Bool?, allowBackgroundEvents: Bool?, useButtonLabels: Bool?). Use `decodeIfPresent` for backward compatibility.

4. Wire the program-level input overrides through the `programUser` layer in constructWineEnvironment (following the same pattern as graphics backend overrides from Phase 4).

5. The inherit/override toggle should show "Inherit from Bottle" summary when all nil, and individual toggle controls when overriding (matching the existing graphics override UX).
  </action>
  <verify>
`xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds. `grep "inputConfig\|controllerCompat" Whisky/Views/Programs/ProgramOverrideSettingsView.swift` returns hits.
  </verify>
  <done>
Per-program input overrides available with inherit/override toggle. Program-level SDL hints take precedence over bottle-level settings when overridden.
  </done>
</task>

</tasks>

<verification>
- `xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds
- `swift test --package-path WhiskyKit` passes (if ProgramOverrides was modified)
- InputConfigSection shows Connected Controllers subpanel
- ProgramOverrideSettingsView has Input override group
</verification>

<success_criteria>
- Connected Controllers subpanel shows controller name + type badge + connection badge + battery (per user decision)
- Bluetooth inline warning "USB is more reliable" shown for wireless controllers
- "No controllers detected" empty state with troubleshooting hint
- XInput vs Native labels toggle available
- Per-program input overrides via inherit/override pattern
- Copy Controller Info for diagnostics export
</success_criteria>

<output>
After completion, create `.planning/phases/08-remaining-platform-issues/08-05-SUMMARY.md`
</output>
