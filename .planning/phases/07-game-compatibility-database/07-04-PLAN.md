---
phase: 07-game-compatibility-database
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - Whisky/Views/GameDB/GameConfigurationView.swift
  - Whisky/Views/GameDB/GameEntryRowView.swift
  - Whisky/Views/Bottle/BottleView.swift
autonomous: true

must_haves:
  truths:
    - "Global Game Configurations view with prominent search box is accessible from BottleView navigation"
    - "Search filters entries by title and aliases case-insensitively"
    - "List rows show title, subtitle, rating badge, backend tag, constraint tags, and one-line note"
    - "Filter controls for store, backend, anti-cheat, and rating are present"
    - "Empty state shows 'No configuration found' message"
    - "Primary click on row navigates to detail view"
  artifacts:
    - path: "Whisky/Views/GameDB/GameConfigurationView.swift"
      provides: "Global game configurations list with search and filters"
      contains: "struct GameConfigurationView"
    - path: "Whisky/Views/GameDB/GameEntryRowView.swift"
      provides: "List row with rating badge, backend tag, constraint tags"
      contains: "struct GameEntryRowView"
    - path: "Whisky/Views/Bottle/BottleView.swift"
      provides: "Navigation link to Game Configurations"
      contains: "gameConfigs"
  key_links:
    - from: "GameConfigurationView"
      to: "GameDBLoader"
      via: "Loads entries on appear"
      pattern: "GameDBLoader\\.loadDefaults"
    - from: "GameConfigurationView"
      to: "GameMatcher.searchEntries"
      via: "Search filtering"
      pattern: "GameMatcher\\.searchEntries"
    - from: "BottleView"
      to: "GameConfigurationView"
      via: "NavigationLink with BottleStage.gameConfigs"
      pattern: "BottleStage\\.gameConfigs"
---

<objective>
Build the global Game Configurations list view with search, filters, and styled list rows. Add navigation entry point from BottleView.

Purpose: This is the primary entry point for the game compatibility database per user decisions. Users can browse, search, and filter game configurations before selecting one to view details or apply.
Output: GameConfigurationView with search/filter, GameEntryRowView with rating badges, BottleView navigation integration.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-compatibility-database/07-RESEARCH.md
@.planning/phases/07-game-compatibility-database/07-02-SUMMARY.md
@Whisky/Views/Bottle/BottleView.swift
@Whisky/Views/Bottle/BackendPickerView.swift
@Whisky/Views/Diagnostics/DiagnosticsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameEntryRowView with rating badge and tags</name>
  <files>
    Whisky/Views/GameDB/GameEntryRowView.swift
  </files>
  <action>
Create `GameEntryRowView` struct conforming to View:

**Properties:**
- `let entry: GameDBEntry`

**Layout (matching CONTEXT.md list view row specification):**
HStack with:
1. **Rating badge** -- colored capsule with CompatibilityRating.displayName text:
   - works: green background
   - playable: yellow/orange background
   - unverified: gray background
   - broken: red background
   - notSupported: dark red/black background
   Use `.font(.caption2)`, `.padding(.horizontal, 6)`, `.padding(.vertical, 2)`, `.clipShape(Capsule())`

2. **Title area** -- VStack(alignment: .leading):
   - Title: `entry.title` in `.headline` font
   - Subtitle: `entry.subtitle ?? entry.store ?? ""` in `.subheadline.foregroundStyle(.secondary)`

3. **Tags area** -- HStack with flow layout:
   - Recommended backend tag: derived from default variant's `settings.graphicsBackend` (e.g., "D3DMetal", "DXVK"). Use Text with `.font(.caption)`, `.padding(4)`, `.background(.blue.opacity(0.1))`, `.clipShape(RoundedRectangle(cornerRadius: 4))`
   - Constraint tags: if `entry.constraints?.cpuArchitectures` contains only "arm64", show "Apple Silicon" tag. If minWineVersion exists, show "Wine {version}+" tag.
   - Anti-cheat tag: if `entry.antiCheat != nil`, show tag with exclamationmark.triangle icon

4. **One-line note** -- `entry.notes?.first` in `.caption.foregroundStyle(.secondary)`, truncated with `.lineLimit(1)`

Use `.contentShape(Rectangle())` for full-row hit target. Apply `.padding(.vertical, 4)` to the row.

Follow the existing app's view patterns: import SwiftUI + WhiskyKit, add GPL v3 header.
  </action>
  <verify>
`xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` compiles without errors.
  </verify>
  <done>
GameEntryRowView displays title, subtitle, rating badge, backend tag, constraint tags, and one-line note matching the CONTEXT.md specification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GameConfigurationView with search and filters, add BottleView navigation</name>
  <files>
    Whisky/Views/GameDB/GameConfigurationView.swift
    Whisky/Views/Bottle/BottleView.swift
  </files>
  <action>
**GameConfigurationView.swift:**
Create `GameConfigurationView` struct:

**Properties:**
- `@ObservedObject var bottle: Bottle` -- target bottle for apply actions
- `@State private var entries: [GameDBEntry] = []` -- loaded entries
- `@State private var searchText: String = ""` -- search query
- `@State private var selectedRating: CompatibilityRating?` -- nil = all ratings
- `@State private var selectedStore: String?` -- nil = all stores
- `@State private var selectedBackend: String?` -- nil = all backends
- `@State private var selectedEntry: GameDBEntry?` -- for NavigationLink detail

**Computed properties:**
- `filteredEntries: [GameDBEntry]` -- apply search (via GameMatcher.searchEntries if searchText non-empty) then apply rating/store/backend filters. If searchText is empty, show all entries with filters applied.

**Body layout:**
NavigationStack-compatible List with:
1. **Search** -- `.searchable(text: $searchText, prompt: "Search games...")` on the list
2. **Filter bar** -- horizontal ScrollView above list with Picker/Menu controls:
   - Rating filter: Picker with "All Ratings" + each CompatibilityRating case
   - Store filter: Picker with "All Stores" + unique store values from entries
   - Backend filter: Picker with "All Backends" + unique backend values
3. **Entry list** -- ForEach over filteredEntries. Each row is a NavigationLink wrapping GameEntryRowView, navigating to GameEntryDetailView (placeholder destination for now -- use `Text("Detail: \(entry.title)")` until Plan 05 creates the real view).
4. **Empty state** -- when filteredEntries is empty:
   - If searchText is non-empty: "No configuration found for '{searchText}'"
   - If no search: "No game configurations available"
   - Both show placeholder icon and text

**Load entries on appear:**
`.onAppear { entries = GameDBLoader.loadDefaults() }`

**Navigation title:** "Game Configurations" with `.navigationTitle`

**BottleView.swift:**
Add `.gameConfigs` case to `BottleStage` enum.

Add NavigationLink in BottleView's Form section (after processes):
```swift
NavigationLink(value: BottleStage.gameConfigs) {
    Label("tab.gameConfigs", systemImage: "gamecontroller")
}
```

Add `.navigationDestination(for: BottleStage.self)` handling for `.gameConfigs`:
```swift
case .gameConfigs:
    GameConfigurationView(bottle: bottle)
```

Ensure the navigation destination is added alongside existing config/programs/processes destinations.
  </action>
  <verify>
`xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` compiles without errors.
Verify BottleView shows "Game Configurations" navigation link.
  </verify>
  <done>
GameConfigurationView shows searchable list of game entries with rating/store/backend filters. Empty state handled. BottleView has navigation link to Game Configurations via BottleStage.gameConfigs. Primary click navigates to detail placeholder.
  </done>
</task>

</tasks>

<verification>
- `xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build` succeeds
- `swiftformat --lint .` passes
- GameConfigurationView loads entries from GameDBLoader
- Search filters entries by title and aliases
- BottleView shows Game Configurations navigation link
- Empty state shows "No configuration found" message
</verification>

<success_criteria>
- List rows display all specified elements: title, subtitle, rating badge, backend tag, constraint tags, note
- Search is case-insensitive on title + aliases
- Filter controls for rating, store, and backend are present
- Empty state renders correctly for both no-results and no-data cases
- BottleView navigation to Game Configurations works
</success_criteria>

<output>
After completion, create `.planning/phases/07-game-compatibility-database/07-04-SUMMARY.md`
</output>
