---
phase: 09-ui-ux-feature-requests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Whisky/Views/ContentView.swift
  - Whisky/Views/Bottle/WineConfigSection.swift
  - Whisky/Views/Bottle/ConfigView.swift
  - WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift
autonomous: true

must_haves:
  truths:
    - "GPTK update dialog shows a single confirmation step (Update Now / Later) and proceeds directly to download without an intermediate dialog"
    - "Build Version displays the actual registry value or 'Not Set' instead of showing '0' for unset bottles"
    - "Retina Mode shows a tri-state toggle (On / Off / Unknown) that does not silently overwrite the user's setting on read failure"
  artifacts:
    - path: "Whisky/Views/ContentView.swift"
      provides: "Simplified GPTK update dialog flow"
      contains: "showSetup = true"
    - path: "WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift"
      provides: "Updated retinaMode returning optional tri-state"
      contains: "retinaMode"
    - path: "Whisky/Views/Bottle/WineConfigSection.swift"
      provides: "Tri-state Retina Mode UI and improved Build Version display"
      contains: "RetinaModeState"
  key_links:
    - from: "Whisky/Views/Bottle/WineConfigSection.swift"
      to: "WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift"
      via: "Wine.retinaMode() and Wine.changeRetinaMode()"
      pattern: "Wine\\.retinaMode|Wine\\.changeRetinaMode"
---

<objective>
Fix GPTK update dialog UX, Build Version display, and Retina Mode tri-state toggle.

Purpose: Address three quick-win UI/UX issues (UIUX-01, UIUX-04) that improve daily usability without introducing new subsystems. These are small, focused fixes to existing views and registry helpers.
Output: Streamlined update dialog, accurate Build Version display, robust Retina Mode toggle.
</objective>

<execution_context>
@/Users/afranke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/afranke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Whisky/Views/ContentView.swift
@Whisky/Views/Bottle/WineConfigSection.swift
@Whisky/Views/Bottle/ConfigView.swift
@WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GPTK update dialog and Build Version/Retina Mode registry layer</name>
  <files>
    Whisky/Views/ContentView.swift
    WhiskyKit/Sources/WhiskyKit/Wine/WineRegistry.swift
  </files>
  <action>
1. **GPTK update dialog (ContentView.swift):** Simplify the update flow in the `.task` body. The current code shows an NSAlert and on confirmation calls `WhiskyWineInstaller.uninstall()` then sets `showSetup = true`. Verify this is truly a single step -- if it is, the fix may be ensuring `SetupView` does not show its own redundant confirmation before starting download. Trace the SetupView flow and remove any secondary "are you sure?" confirmation that creates a two-step experience. The end result: user sees one alert ("Update available: vX -> vY. Update Now / Later"), clicks "Update Now", and the download/install begins immediately.

2. **Build Version (WineRegistry.swift):** Modify `Wine.buildVersion(bottle:)` to return `String?` (it already does). The display fix is in the UI layer -- no changes needed here unless the method swallows nil incorrectly.

3. **Retina Mode (WineRegistry.swift):** Modify `Wine.retinaMode(bottle:)` to return `Bool?` instead of `Bool`. Currently when the registry query fails or returns an unexpected value, it forces a write of `false` and returns `false` -- this silently overwrites the user's setting. Change to:
   - If registry returns "y" -> return `true`
   - If registry returns "n" -> return `false`
   - If registry returns nil or unexpected value -> return `nil` (do NOT write anything)
   - Remove the forced `changeRetinaMode(bottle:retinaMode:false)` call on failure
   - This is a breaking signature change, so also update `Wine.changeRetinaMode` to accept `Bool` (no change needed there).
  </action>
  <verify>Build WhiskyKit with `swift build --package-path WhiskyKit` -- no errors. The `retinaMode` method now returns `Bool?`.</verify>
  <done>retinaMode returns nil on read failure instead of silently writing false; GPTK dialog is single-step.</done>
</task>

<task type="auto">
  <name>Task 2: Tri-state Retina Mode UI and Build Version display fix</name>
  <files>
    Whisky/Views/Bottle/WineConfigSection.swift
    Whisky/Views/Bottle/ConfigView.swift
  </files>
  <action>
1. **Define RetinaModeState** at file scope in WineConfigSection.swift (or a small helper):
   ```
   enum RetinaModeState: Equatable {
       case on, off, unknown
   }
   ```

2. **WineConfigSection.swift:** Change `@Binding var retinaMode: Bool` to `@Binding var retinaModeState: RetinaModeState`. Update the Retina Mode row:
   - Replace the `Toggle` with a `Picker` or segmented control showing On / Off / Unknown.
   - When state is `.unknown`, show a caption: "Registry value could not be read. You can set it explicitly."
   - When user selects On or Off, call `Wine.changeRetinaMode(bottle:retinaMode:)`, then re-read to confirm. Update loading state accordingly.
   - On `.unknown`, allow the user to pick On or Off (write-through per user decision).

3. **ConfigView.swift:** Update `loadRetinaMode()` to handle `Bool?`:
   - `nil` -> set `retinaModeState = .unknown`
   - `true` -> set `retinaModeState = .on`
   - `false` -> set `retinaModeState = .off`
   - Change `@State private var retinaMode: Bool = false` to `@State private var retinaModeState: RetinaModeState = .unknown`
   - Update all bindings passed to WineConfigSection.

4. **Build Version display (ConfigView.swift):** In `loadBuildName()`, when `Wine.buildVersion()` returns nil, set a display string like "Not Set" instead of defaulting to `0`. Change `@State private var buildVersion: Int = 0` to `@State private var buildVersion: String = ""`. Update WineConfigSection to use a `TextField` with string binding. When the user submits a value, parse it as Int and call `Wine.changeBuildVersion`. Show "Not Set" placeholder when empty.

5. Add localization keys: `config.retinaMode.on`, `config.retinaMode.off`, `config.retinaMode.unknown`, `config.retinaMode.unknownHint`, `config.buildVersion.notSet`.
  </action>
  <verify>Build Whisky app target with `xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build`. No errors.</verify>
  <done>Retina Mode shows tri-state picker (On/Off/Unknown) with write-through. Build Version shows actual string value or "Not Set" placeholder. GPTK dialog is streamlined.</done>
</task>

</tasks>

<verification>
- Build Whisky app target: `xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug build`
- Run WhiskyKit tests: `swift test --package-path WhiskyKit`
- Verify `Wine.retinaMode` returns `Bool?` (grep for the signature)
- Verify no forced write on read failure in retinaMode (grep for `changeRetinaMode` calls inside retinaMode method -- should be removed)
</verification>

<success_criteria>
- GPTK update flow is a single confirmation step
- Build Version displays actual registry value or "Not Set" for unset bottles
- Retina Mode uses tri-state (On/Off/Unknown) without silently overwriting on failure
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-feature-requests/09-01-SUMMARY.md`
</output>
